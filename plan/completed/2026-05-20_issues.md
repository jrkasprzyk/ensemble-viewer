---
goal: fix issues associated with recent enhancements
version: 1.0
date_created: 2026-05-20
last_updated: 2026-05-20
owner: Joseph Kasprzyk
status: 'Completed'
---

The result of the last change was this:

67/67, build clean. Summary of changes:

  - Sort: dropdown now shows original pre-tie categories (baseCategoryValues) — user picks "Historical Year" or
  "Historical Year Percent of Average" separately; buildSortMetadata uses labelsByColumn (clean numeric strings, no
  |)
  - Swatches: colorMap computed once in App.jsx (sequential for numeric colorBy, qualitative otherwise), passed to
  both LabelControls and EnsemblePlot — swatches now match plot colors exactly
  
But there are still some issues. The coloring doesn't seem to be using the proper colormap now. The sort/filter by correctly shows both tied categories, but the actual sort itself doesn't seem to work. The filter also doesn't work, you can select, say, 100 as the min but there are still traces that have 78, 95, etc.

--

These issues have been resolved; will need to do further testing to make sure every sub-feature is working properly, though